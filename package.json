{
  "name": "ai-digital-teacher-supervision",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \".venv\\Scripts\\python.exe -m uvicorn backend.app.main:app --reload --reload-dir backend --reload-dir data --port 8000\" \"npm --prefix frontend run dev -- --port 5173 --strictPort\"",
    "test": "npm --prefix frontend test && .venv\\Scripts\\python.exe -m pytest backend/tests",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
